# TODO: Доработка «SmartGrid Control Center» до 100% соответствия ТЗ

Ниже представлен список доработок, необходимых для успешной сдачи проекта в соответствии с техническим заданием (с учётом того, что реальные устройства не используются, а система работает на mock-подключениях).

## 🔒 Безопасность и Инфраструктура

- [x] **Обновление уязвимых зависимостей (`bun audit`)**
  - Устранить 24 уязвимости в сторонних пакетах (включая 8 уязвимостей высокого уровня в `next`, `@vitejs/plugin-rsc`, `turbo` и др.).
  - *Решение:* Запустить `bun update` или выборочно обновить проблемные пакеты до безопасных стабильных версий.

## 👤 Пользователи и Авторизация

- [x] **Проверка ролей на стороне Frontend (UI)**
  - Маршруты администрирования (страница `/admin/users`) должны быть недоступны для пользователей с обычной ролью `user` на уровне интерфейса.
  - *Решение:*
    - Добавить декодирование JWT-токена в `apps/frontend/src/proxy.ts` для считывания роли и перенаправления не-master пользователей.
    - Внедрить проверку роли внутри страницы `admin/users/page.tsx` с редиректом или отображением экрана ошибки «Недостаточно прав».

## 🔌 Управление устройствами и Интерфейс

- [x] **Вывод поля «Võimsus (kW) / Мощность (кВт)» в форму устройств**
  - Поле `powerConsumption` присутствует в БД Drizzle и TypeBox валидаторе, но полностью отсутствует в HTML-форме `DeviceForm.tsx`. Из-за этого при создании прибора его мощность всегда равна `null`.
  - *Решение:* Добавить поле ввода `powerConsumption` (числовое, с шагом `0.01` кВт) в `DeviceForm.tsx`, чтобы пользователи могли указывать реальное потребление каждого прибора при добавлении/редактировании.

## 📈 Прогноз и Планирование

- [x] **Интеграция плана автоматического переключения приборов**
  - На странице `/forecast` отображаются только цены Elering, но отсутствует интерактивный план включения/выключения приборов по их индивидуальным порогам.
  - *Решение:* Загрузить mock-устройства пользователя через API, сопоставить их пороговые значения (`threshold`) с прогнозируемыми ценами на 24 часа вперед и вывести наглядную временную шкалу (план работы) для каждого прибора.

## 💰 Экономия и Отчетность

- [x] **Корректный расчет сбережений по ТЗ**
  - Сервис `calculateSavings` игнорирует показатели мощности приборов (`powerConsumption`) и время их работы, суммируя разницу тарифов напрямую за каждый час (что рассчитывает сбережения на 1 кВт абстрактной мощности).
  - *Решение:*
    - Переписать алгоритм с учетом реального времени работы приборов (анализируя историю логов `deviceCommandsLog` — когда прибор переключался в состояние ON и OFF).
    - Умножать сэкономленный тариф на мощность прибора (`powerConsumption`), чтобы рассчитать реальную выгоду в евро.

## 🌴 Режим отпуска (Vacation Mode)

- [x] **Интеграция состояния и восстановление режимов Auto/Manual**
  - При выходе из режима отпуска все приборы принудительно переводятся во включенное состояние через `toggle` (что включает режим ручного управления `overrideActive: true`). Это ломает исходные настройки приборов, которые до отпуска управлялись автоматически.
  - *Решение:*
    - Перед переходом в Vacation Mode сохранять в Zustand-хранилище фронтенда (`settingsStore.ts`) полную конфигурацию прибора (не только ID, но и его исходные значения `overrideActive` и `overrideState`).
    - При выключении режима отпуска восстанавливать исходные параметры для каждого устройства индивидуально.

## 🧪 Тестирование

- [x] **Покрытие юнит-тестами middleware бэкенда**
  - Код middleware авторизации и валидации роли (`apps/backend/src/middleware/auth.ts` и `role.ts`) не покрыт автоматическими тестами.
  - *Решение:* Добавить тесты в `apps/backend/tests/auth.test.ts` (или вынести в отдельный файл), проверяющие поведение Elysia-сервера при отправке запросов неавторизованным пользователем или пользователем без роли `master`.

## 🐈👑 Глобальная котофикация! (Cats must be everywhere)

- [x] **Коты повсюду! 🐾🐾**
  - Проект должен быть полностью переработан под кошачью тематику (Cat-themed SmartGrid), так как котиков много не бывает!
  - *Решение:*
    - **Логотип и брендинг:** Заменить молнию в логотипе SmartGrid на иконку кошачьей лапки или кошачьей мордочки (например, `Cat` или `PawPrint` из Lucide React).
    - **Глобальные загрузчики:** Заменить все стандартные спиннеры и пульсирующие скелетоны на весёлые анимированные гифки с бегущими или танцующими котами.
    - **Пустые экраны:** Все сообщения об отсутствии данных («нет устройств», «нет логов», «нет цен») оформить смешными картинками с котами в растерянности.
    - **Иконки состояний приборов:** Вместо банальных переключателей использовать забавных котиков (например: спящий котик 💤 = прибор выключен; бодрствующий весёлый котик с молниями ⚡ = прибор включен).
    - **Кошачий режим отпуска:** Переименовать «Режим отпуска» в «Режим ленивого кота» (Lazy Cat Mode) с соответствующими кошачьими смайликами.
    - **Текстовые вставки:** Добавить забавные кошачьи фразы (мяуканье / мурчание) в подсказки интерфейса (например: «Успешно сохранено! Мяу!»).
